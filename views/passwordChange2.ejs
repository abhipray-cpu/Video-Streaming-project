<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Confirm Password</title>
    <link rel="stylesheet" href="/css/passwordChange1.css">
</head>

<body>
    <% if(error.length>0) { %>
        <div id="Error">
            <h3>
                <%= error %>
            </h3>
        </div>
        <% } %>
            <div id="form">
                <form action="/confirmChange" method="POST">
                    <input type="hidden" value="<%= csrfToken %>" name="_csrf">
                    <!--the name here is specific for the csrf token and therfore should not be changed-->
                    <div class="form-control">
                        <label for="password" style="color:white;font-weight:bolder;font-size:x-large">New Password</label>
                        <input type="password" id="password" name="password">
                    </div>
                    <div class="form-control">
                        <label for="password" style="color:white;font-weight:bolder;font-size:x-large">Renter Password</label>
                        <input type="password" id="Confirm" name="Confirm">
                    </div>

                    <input type="hidden" name="userId" id="userId" value="<%= userId %>">
                    <div id="btn">
                        <button type="submit">Change Password</button>
                    </div>
                </form>
            </div>
</body>

</html>